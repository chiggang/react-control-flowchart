/**
 * Configure item
 * @Type
 */
export enum EConfigureItem {
  NODE_HEADER = 'node.header',
  NODE_SIZE = 'node.size',
}

/**
 * Stroke animation
 * @Type
 */
export enum EStrokeAnimation {
  NONE = 'none',
  LINE_FLOW = 'line-flow',
  DASH_FLOW = 'dash-flow',
  FLASH = 'flash',
}

/**
 * InOutput
 * @Type
 */
export enum EInOutput {
  INPUT = 'input',
  OUTPUT = 'output',
}
